# Binaries (local dev only)
*.exe~
*.dll
*.so
*.dylib

# Test binary
*.test

# Output
*.out

# IDE
.idea/
.vscode/
*.swp
*.swo

# OS
.DS_Store
Thumbs.db

# npm
node_modules/

# Local dev binaries (dist/ is tracked for npm)
itak-shield
goproxy.exe
itak-shield.exe

# ── Secrets & runtime state (NEVER commit) ──────────────────
# Auth databases (SQLite user/token store)
shield.db
shield.db-shm
shield.db-wal
shield-users.json
shield-users.json.tmp
shield-users.json.migrated

# Config with real API keys
shield.yaml
shield.yml
.env
.env.*
*.local.yaml

# Audit logs (may contain request metadata)
audit.jsonl
audit.jsonl.*
audit/
logs/

# Backups
*.bak
*.db.bak
